// Placeholder for generatedAccessToken utility
// Replace with real JWT/token generation logic as needed
export default function generatedAccessToken(user) {
    // Return a dummy token for now
    return 'dummy-access-token';
}
